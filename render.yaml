services:
  - type: web
    name: rift-mart-payment
    env: python
    rootDir: payment
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn api:app --host 0.0.0.0 --port $PORT
